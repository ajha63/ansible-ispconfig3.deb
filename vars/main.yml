---
# vars file for ispconfig3.deb
ispconfig3_ip: 
  - 192.168.1.1

ispconfig3_fqdn: 
  - example.com

# packages name
sshd_packages:
  - openssh-server

vim-nox_packages:
  - vim-nox

sshd_service: 
  - ssh

sshd_file_config: 
  - /etc/ssh/sshd_config